<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- Reading saved Wi-Fi networks needs this from Android 10; the kiosk grants it to itself at provisioning. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Reading the trainer's provisioning code to update a phone already in
         service. Granted by the kiosk to itself the moment before the scanner
         opens, since under lock task a permission dialog is one more thing that
         can go wrong in front of someone who cannot read it. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_kiosk"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kiosk">

        <meta-data android:name="io.sentry.dsn" android:value="${sentryDsn}" />

        <!--
          Nothing runs at process start: the launcher is the HOME activity and
          anything that delays its first window past the input-dispatch grace
          period gets it ANR-killed at boot (WorkManager's initialiser once did
          exactly that). Measure before adding any startup work.
        -->
        <!--
          From Android 11 the setup wizard asks the DPC for these two before it
          will finish a QR provisioning run. Both are answered without any UI; the
          BIND_DEVICE_ADMIN permission is what keeps other apps from starting
          them, since they have to be exported for the wizard to reach them.
        -->
        <activity
            android:name=".ProvisioningModeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:theme="@style/Theme.Kiosk">
            <intent-filter>
                <action android:name="android.app.action.GET_PROVISIONING_MODE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PolicyComplianceActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:theme="@style/Theme.Kiosk">
            <intent-filter>
                <action android:name="android.app.action.ADMIN_POLICY_COMPLIANCE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".ProvisioningService"
            android:exported="false"
            android:foregroundServiceType="dataSync">
            <!-- Lets the launcher's "set this phone up again" reach the service
                 without the launcher module depending on this one. -->
            <intent-filter>
                <action android:name="org.awana.kiosk.action.SET_UP_AGAIN" />
            </intent-filter>
            <intent-filter>
                <action android:name="org.awana.kiosk.action.APPLY_UPDATE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".KioskDeviceAdminReceiver"
            android:description="@string/device_admin_description"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.PROFILE_PROVISIONING_COMPLETE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
